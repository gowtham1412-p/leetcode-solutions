        }
            while(sum>=target){
              res=Math.max(res,j-i+1);
            }
           
              sum-=nums[i];
              i++;
            sum+=nums[j];
      return res==Integer.MAX_VALUE ? 0:res;
        for(int j=0;j<n;j++){
        int res=Integer.MAX_VALUE;
        int sum=0;
        
}
}
