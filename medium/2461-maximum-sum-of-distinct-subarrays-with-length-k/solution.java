           if(j-i+1==k){
                sum+=j-i;
           }
        }
                max=Math.max(max,sum);
                set.remove(nums[i]);
                i++;
           j++;

        }
           set.add(nums[i]);
            if(!set.contains(nums[j])){
        while(j<n){
        int j=0;
        int i=0;
        return max;
