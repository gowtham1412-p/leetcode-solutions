           if(j-i+1==k){
                sum+=j-i;
                max=Math.max(max,sum);
                set.remove(nums[i]);
                i++;
           while(set.contains(nums[j])){
        while(j<n){
        int j=0;
        int i=0;
        HashSet<Integer>set=new HashSet<>();
        int sum=0;
        long max=0;
            
            set.remove(nums[i]);
           }
            i++;
           set.add(nums[j]);
