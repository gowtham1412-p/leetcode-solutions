                set.remove(nums[i]);
           while(!set.contains(nums[j])){
            
           set.add(nums[j]);
           sum+=nums[j];
                i++;
           if(j-i+1==k){
                max=Math.max(max,sum);
           }
                sum-=nums[i];
        while(j<n){
        int j=0;
           j++;
        }
        return max;
           }
