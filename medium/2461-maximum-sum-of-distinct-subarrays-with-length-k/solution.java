           while(set.contains(nums[j])){
            
           set.remove(nums[i]);
           sum-=nums[i];
           if(j-i+1==k){
                max=Math.max(max,sum);
           }
        while(j<n){
        int j=0;
           set.add(nums[j]);
           sum+=nums[j];
                set.remove(nums[i]);
                sum-=nums[i];
                i++;
           }
           j++;
           i++;
