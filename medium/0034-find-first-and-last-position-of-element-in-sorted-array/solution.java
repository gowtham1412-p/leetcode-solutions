
               
                right=mid-1;
                first=mid;
            if(mid==target){
            int mid=left+(right-left)/2;
        while(left<=right){
       int right=n-1;
        int n=nums.length;
       int left=0;
       int first=-1, last=-1;
    public int[] searchRange(int[] nums, int target) {
class Solution {
       if(target==0)
          return new int[]{-1,-1};
