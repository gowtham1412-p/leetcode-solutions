                list.add(temp.get(i));
              }
              for(int i=temp.size()-1;i>=0;i--){
              }
                num/=10;

        }
        int arr[]=new int[list.size()];
        for(int i=0;i<list.size();i++){
            arr[i]=list.get(i);
        }
        return arr;
    }
}
