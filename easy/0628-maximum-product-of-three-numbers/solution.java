class Solution {
    public int maximumProduct(int[] nums) {
        int pro=1;
        int n=nums.length;
        for(int i=0;i<n;i++){
            pro*=nums[i];
        max=Math.max(pro,nums[i]);
    }
        }
        return max;
        int max=0;
}
